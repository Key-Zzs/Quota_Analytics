class AppConstants {
  const AppConstants._();

  static const appName = 'Quota Analytics';
  static const appSubtitle =
      'Unofficial personal tool / foreground WebView refresh + local parser';
  static const appMode = 'stage7-foreground-webview-refresh';
  static const stageNotice =
      'Stage 7: foreground-only WebView refresh from redacted visible text only. No cookie/token/storage access or background refresh.';
  static const safetyNotice =
      'No password storage, cookie/token/localStorage/sessionStorage reading, HTML extraction, network upload, or background refresh.';
}
