class AppConstants {
  const AppConstants._();

  static const appName = 'Quota Analytics';
  static const appSubtitle =
      'Unofficial personal tool / manual WebView refresh + local quota parser';
  static const appMode = 'stage6-manual-webview-refresh';
  static const stageNotice =
      'Stage 6: user-triggered WebView manual refresh from redacted visible text only. No cookie/token/storage access, automatic refresh, or background refresh.';
  static const safetyNotice =
      'No password storage, cookie/token/localStorage/sessionStorage reading, HTML extraction, network upload, automatic refresh, or background refresh.';
}
